<%=
    user = event_holiday_request.target
    person_in_power = event_holiday_request.trigger
    status = event_holiday_request.status
    t(
      '.full_html',
      user: user.name,
      status: t('.'+status),
      person_in_power: person_in_power.name,
      )
  %>
