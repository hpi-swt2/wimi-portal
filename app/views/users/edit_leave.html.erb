<%= form_for(@user) do |f| %>
  <div class="panel panel-default">
    <div class="panel-heading">
      <h1><%= t('.editYourLeave') %></h1>
    </div>
    <div class="panel-body">
      <div class="form-group">
        <%= f.label "Remaining Leave", class: 'control-label' %>
        <%= f.number_field :remaining_leave, value: @user.remaining_leave, class: 'form-control' %>
        <%= error_span(@user[:remaining_leave]) %>
      </div>
      <div class="form-group">
        <%= f.label "Remaining Leave Left From Last Year", class: 'control-label' %>
        <%= f.number_field :remaining_leave_last_year, value: @user.remaining_leave_last_year, class: 'form-control' %>
        <%= error_span(@user[:remaining_leave_last_year]) %>
      </div>
      <%= f.submit nil, class: 'btn btn-primary' %>
    </div>
  </div>
<% end %>
