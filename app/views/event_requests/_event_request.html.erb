<%= l event_request.created_at %> - 
<%= t(event_request.status, scope: 'events.event_request', trigger: event_request.trigger.name) %>
<%= link_to t('helpers.links.show'), show_event_request_path(id: event_request, request_id: event_request.target_id, status: event_request.status), method: :post, class: 'label label-success' %>
<%= link_to t('helpers.submit.hide'), hide_event_path(id: event_request, request: current_user), method: :post, class: 'label label-warning' %><br/>
