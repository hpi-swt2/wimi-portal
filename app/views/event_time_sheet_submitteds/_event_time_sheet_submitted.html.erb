<% time_sheet = TimeSheet.find(event_time_sheet_submitted.trigger_id) %>
<%= t('.full_html',
    link_to_user: link_to(time_sheet.user.name, time_sheet.user),
    link_to_view: link_to(t('.view'), time_sheet_url(time_sheet))
) %>
