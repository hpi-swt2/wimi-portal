<%- model_class = Project -%>
<div class="col-md-12 col-sm-12">
  <div class="display">
    <div class="page-header">
      <h1><%=t '.title', default: [:'helpers.titles.edit', 'Edit %{model}'], model: model_class.model_name.human.titleize %>: <%= @project.title %> <span> <%= @project.status ? '' : t('.inactive', default: t("projects.show.inactive")) %></span></h1>
    </div>
    <%= render partial: 'form' %>
  </div>
</div>
