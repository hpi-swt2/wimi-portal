<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @trip.name %>
</p>

<p>
  <strong>Destination:</strong>
  <%= @trip.destination %>
</p>

<p>
  <strong>Reason:</strong>
  <%= @trip.reason %>
</p>
<ul>
	<%= render @trip.trip_datespans %>
</ul>
<p>
  <strong>Annotation:</strong>
  <%= @trip.annotation %>
</p>

<p>
  <strong>Signature:</strong>
  <%= @trip.signature %>
</p>

</p>
  <strong>Status:</strong>
  <%= @trip.status =%>
  <% if @trip.status == 'declined' %>
    by:
  <% end %>
</p>

<% if @trip.status != 'applied' %>
  <%= link_to 'Edit', edit_trip_path(@trip) %> |
  <%= link_to 'Apply', trips_apply_path(id: @trip), method: post %> |
<%end%>

<%= link_to 'Back', trips_path %> |
<%= link_to 'Download', generate_pdf_path(doc_type: 'Dienstreiseantrag', doc_id: @trip) %>

<p>
  <strong>Reisekosten</strong>
  <%= link_to 'Reisekosten eintragen', edit_travel_expense_report_path() %> <!--TODO: zum richtigen Report linken -->
</p>

