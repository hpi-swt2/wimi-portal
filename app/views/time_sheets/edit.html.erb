<div class="col-md-12">
  <div class="display">
    <div class="page-header">
      <div class="h1">
        <div class="row">
          <div class="col-sm-9">
            <%= t('.title') %>
          </div>
          <div class="col-sm-3">
            <%= link_to t('helpers.links.back'), work_days_path(month: @time_sheet.month, year: @time_sheet.year, contract: @time_sheet.contract), class: 'btn btn-default btn-block'%>
          </div>
        </div>
      </div>
    </div>
    <%= render 'form' %>
  </div>
</div>
