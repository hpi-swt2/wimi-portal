<% current_user.projects.each do |project| %>
  <p>
    <%= link_to(
      t('.action', project_name: project.title),
      work_days_path(
        month: Date.today.month,
        year: Date.today.year,
        project: project.id))
    %>
  </p>
<% end %>
