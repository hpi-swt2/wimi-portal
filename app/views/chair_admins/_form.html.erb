<%= form_for(@chair_admin) do |f| %>
  <% if @chair_admin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chair_admin.errors.count, "error") %> prohibited this chair_admin from being saved:</h2>

      <ul>
      <% @chair_admin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :chair_id %><br>
    <%= f.text_field :chair_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
