<%- model_class = Expense -%>
<div class="col-md-12">
	<div class="display">
		<h1>
			<div class="page-header">
			<div class="row">
				<div class="col-md-10">
					<%=t '.title', default: [:'helpers.titles.new', 'Edit %{model}'], model: model_class.model_name.human.titleize %>
				</div>
				<div class="col-md-2">
					<%= link_to t('helpers.links.back'), @expense.trip , class: "btn btn-default btn-xs"%>
				</div>
			</div>
			</div>
		</h1>
		<%= render 'form' %>
	</div>
</div>
